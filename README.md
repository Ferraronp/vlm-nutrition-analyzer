# Food Nutrition Analysis API

Анализатор пищевой ценности блюд на основе ИИ, который использует Vision-Language Model (VLM) для распознавания продуктов на изображениях и Large Language Model (LLM) для расчета калорий и БЖУ.

## 🚀 Особенности

- **Двухэтапный анализ**: VLM распознает продукты на изображении, LLM рассчитывает пищевую ценность
- **Поддержка URL изображений**: Просто укажите прямую ссылку на изображение блюда
- **Точный анализ**: VLM предоставляет количественные оценки каждого компонента блюда
- **Расчет БЖУ**: Автоматический расчет калорий, белков, жиров и углеводов
- **Мониторинг**: Встроенные метрики производительности и использования ресурсов
- **Масштабируемость**: Поддержка развертывания на нескольких Google Colab инстансах с балансировкой нагрузки

## 📋 Требования

- Python 3.8+
- GPU с CUDA (рекомендуется для VLM модели)
- Groq API ключ
- 8GB+ RAM (рекомендуется 16GB+)

## 🔧 Установка и запуск в Google Colab

### Быстрый старт (одна сессия)

1. Откройте [colab.ipynb](colab.ipynb) в Google Colab
2. Запустите все ячейки по порядку
3. После запуска получите публичный URL

### Развертывание нескольких экземпляров (для ускорения)

Для ускорения обработки запросов можно запустить несколько экземпляров API на разных Google Colab сессиях:

#### Шаг 1: Подготовка нескольких сессий

1. Откройте **несколько вкладок** Google Colab (рекомендуется 2-4)
2. В каждой вкладке загрузите [colab.ipynb](colab.ipynb)

#### Шаг 2: Запуск в каждой сессии

В каждой сессии выполните:

```python
# Установка зависимостей
!pip install -q -r requirements.txt

# Клонирование репозитория (если не склонировано)
!git clone https://github.com/Ferraronp/vlm-nutrition-analyzer
%cd vlm-nutrition-analyzer

# Настройка API ключа
%%writefile .env
GROQ_API_KEY="your_groq_api_key_here"

# Запуск сервера
!python app.py
```

#### Шаг 3: Получение URL для каждой сессии

После запуска в каждой сессии получите уникальный URL:

**Сессия 1**: `https://abc123.loca.lt`  
**Сессия 2**: `https://def456.loca.lt`  
**Сессия 3**: `https://ghi789.loca.lt`  
**Сессия 4**: `https://jkl012.loca.lt`

#### Шаг 4: Балансировка нагрузки

Для автоматической балансировки между экземплярами используйте встроенный балансировщик нагрузки:

```bash
# Запуск балансировщика
python load_balancer.py
```

Балансировщик будет доступен на `http://localhost:8080` и автоматически распределяет запросы между активными экземплярами.

**Основные функции балансировщика:**
- Добавление/удаление экземпляров через REST API
- Поддержка cookies (включая `colab-runtime-proxy-token`)
- Автоматическая проверка активности экземпляров
- Удаление неактивных экземпляров после 3 неудачных попыток
- Распределение нагрузки между активными экземплярами
- Статистика и мониторинг

**API эндпоинты балансировщика:**
- `POST /instances` - добавить экземпляр
- `DELETE /instances/{id}` - удалить экземпляр
- `GET /instances/{id}` - получить информацию об конкретном экземпляре
- `GET /instances` - список всех экземпляров
- `GET /analyze` - анализирование картинки через балансировщик
- `GET /health` - состояние балансировщика
- `GET /stats` - статистика

## 📡 API Эндпоинты

### Основной эндпоинт анализа

```http
GET /?image_url=https://example.com/food.jpg
```

**Ответ:**
```json
{
  "status": "success",
  "data": {
    "analysis": "1. Рис отварной (100 г) — 130 ккал, белки: 2.7 г, жиры: 0.3 г, углеводы: 28 г\n2. Куриная грудка (100 г) — 165 ккал, белки: 31 г, жиры: 3.6 г, углеводы: 0 г\n\nОбщая калорийность: ~295 ккал...",
    "vlm_output": "COMPONENTS:\n1. White rice: ~200ml, covers 60% of plate\n2. Chicken breast: 2 large pieces, ~150g total..."
  }
}
```

### Проверка работоспособности

```http
GET /health
```

### Мониторинг метрик

```http
GET /metrics/system
```

## 🔧 Локальная установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Ferraronp/vlm-nutrition-analyzer
cd vlm-nutrition-analyzer
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте переменные окружения:
```bash
echo "GROQ_API_KEY=your_groq_api_key_here" > .env
```

4. Запустите сервер:
```bash
python app.py
```

## ⚙️ Конфигурация

Основные параметры в `config.py`:

- `MODEL_NAME`: VLM модель (по умолчанию: "Qwen/Qwen2.5-VL-7B-Instruct")
- `HOST` и `PORT`: Параметры сервера (по умолчанию: 0.0.0.0:8000)
- `MAX_NEW_TOKENS`: Максимальное количество токенов для VLM
- `TEMPERATURE`: Температура для LLM
- `MAX_COMPLETION_TOKENS`: Максимальное количество токенов для LLM

## 🎯 Примеры использования

### Python

```python
import requests

# URL одного из ваших экземпляров
BASE_URL = "https://abc123.loca.lt"

# Анализ изображения
response = requests.get(f"{BASE_URL}/", params={
    "image_url": "https://example.com/borscht.jpg"
})

result = response.json()
print(result["data"]["analysis"])
```

### cURL

```bash
curl "https://abc123.loca.lt/?image_url=https://example.com/pizza.jpg"
```

## 📊 Производительность

- **Время обработки одного изображения**: 15-25 секунд
- **Оптимальное количество параллельных экземпляров**: 2-4
- **Максимальная нагрузка на один экземпляр**: ~4 запросов в минуту

## 🔍 Как это работает

1. **VLM этап**: Модель Qwen2.5-VL анализирует изображение и предоставляет детальное описание всех компонентов блюда с количественными оценками

2. **LLM этап**: Groq API обрабатывает описание и рассчитывает пищевую ценность на основе стандартных таблиц калорийности

3. **Результат**: Получаете развернутый анализ с калориями, белками, жирами и углеводами

## 🛠️ Разработка

### Структура проекта

```
├── app.py              # FastAPI сервер
├── vlm_service.py      # VLM сервис (Qwen2.5-VL)
├── llm_service.py      # LLM сервис (Groq)
├── model_loader.py     # Загрузка VLM модели
├── load_balancer.py    # Балансировщик нагрузки
├── config.py           # Конфигурация
├── prompts.py          # Промпты для VLM и LLM
├── memory_monitor.py   # Мониторинг ресурсов
└── colab.ipynb         # Google Colab notebook
```

### Тестирование

```bash
# Проверка работоспособности
curl http://localhost:8000/health

# Тестовый запрос
curl "http://localhost:8000/?image_url=https://example.com/test.jpg"
```

## ⚠️ Важные замечания

- **API ключи**: Храните ваши Groq API ключи в безопасности
- **Лимиты**: Учитывайте лимиты на бесплатные Google Colab сессии (до 6 часов)
- **GPU**: Для VLM модели требуется GPU с CUDA
- **Точность**: Расчеты являются приблизительными и зависят от качества изображения

---

**Разработано с ❤️ для анализа пищевой ценности блюд с помощью ИИ**